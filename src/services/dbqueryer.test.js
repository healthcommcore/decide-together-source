const firebase = require('firebase/app');
const firestore = require('firebase/firestore');
const firebaseConfig = require('../firebase.config.js');
const dbQueryer = require('./dbqueryer');

it('Should do something', () => {
});

